const classroomAddressInfoLoader = async () => {
  // fetch data of class from backend server;

  console.log(" fetch data of class from backend server");
  return null;
};

export { classroomAddressInfoLoader };
